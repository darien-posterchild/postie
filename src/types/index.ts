// Common TypeScript types for the prototype

export interface BaseEntity {
  id: string;
  createdAt?: string;
  updatedAt?: string;
}

// Additional domain types will be defined here as Figma screens are implemented.
